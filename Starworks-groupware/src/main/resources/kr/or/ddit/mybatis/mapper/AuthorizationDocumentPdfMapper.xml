<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
 * == 개정이력(Modification Information) ==
 *   
 *   수정일      			수정자            수정내용
 *  ============   	============== =======================
 *  2025. 10. 2.     	임가영            최초 생성
 *
-->
<mapper namespace="kr.or.ddit.mybatis.mapper.AuthorizationDocumentPdfMapper">

<select id="selectAuthorizationDocumentPdf" resultType="kr.or.ddit.vo.AuthorizationDocumentPdfVO">
	SELECT
		ATRZ_DOC_ID
		, SAVE_FILE_NM
		, FILE_PATH
		, EXT_FILE
		, FILE_SIZE
		, FILE_MIME_TYPE
		, CRT_DT
	FROM 
		AUTHORIZATION_DOCUMENT_PDF
	WHERE 
		ATRZ_DOC_ID = #{atrzDocId}
</select>

<insert id="insertAuthorizationDocumentPdf">
	INSERT INTO
		AUTHORIZATION_DOCUMENT_PDF (
			ATRZ_DOC_ID
			, SAVE_FILE_NM
			, FILE_PATH
			, EXT_FILE
			, FILE_SIZE
			, FILE_MIME_TYPE
		) VALUES (
			#{atrzDocId}
			, #{saveFileNm}
			, #{filePath}
			, #{extFile}
			, #{fileSize}
			, #{fileMimeType}
		)
</insert>
</mapper>