<?xml version="1.0" encoding="UTF-8"?>
<!-- 
 * == 개정이력(Modification Information) ==
 *   
 *   수정일      			수정자           수정내용
 *  ============   	============== =======================
 *  2025. 9. 25.     	임가영           최초 생성
 *
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.mybatis.mapper.FeaturesMapper">

<select id="selectFeaturesList" resultType="kr.or.ddit.vo.FeaturesVO">
	SELECT 
		FEATURE_ID
		, FEATURE_NAME
	FROM
		FEATURES
</select>

<select id="selectFeatures" resultType="kr.or.ddit.vo.FeaturesVO">
	SELECT 
		FEATURE_ID
		, FEATURE_NAME
	FROM
		FEATURES
	WHERE
		FEATURE_ID = #{featureId}
</select>


</mapper>